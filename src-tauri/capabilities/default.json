{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bundled-splash",
  "description": "Only the bundled DigiBox startup page may subscribe to desktop state events.",
  "windows": ["splash"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
